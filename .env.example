# Copy to .env to change. Defaults shown.

# Host interface for the HTTP port. 0.0.0.0 exposes it to your network.
HTTP_BIND=127.0.0.1

# Host port forwarded to Traefik.
HTTP_PORT=8080

# HTTPS URL subdocker-1 fetches at startup.
TARGET_URL=https://example.com
